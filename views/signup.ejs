<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/signup_style.css" />
    <title>Forty Days</title>
</head>
<body>
    <div class="container">
        <h2>SIGN UP</h2>

        <form class="signup_form" id="signup_form" action="/signup" method="POST">
            <% if (messages.error) { %>
                <div class="alert"> <strong><%= messages.error %> </strong></div>
            <% } %>
            <input
                class="field"
                id="email"
                name="email"
                type="email"
                required
                placeholder="Email"/><br>
            <input class="field"
                id="username"
                name="username"
                type="text"
                required
                placeholder="Username"/><br>

            <input class="field"
                id="password"
                name="password"
                type="password"
                required
                placeholder="Password"
                minlength="6"
                maxlength="30"/><br>

            <input 
                class="submit-signup"
                type="submit"
                value="Register"/>

        </form>
        <div>
            <p>Already have an account? <a class="switch-login" href='/'>Log in</a></p>
        </div>


    </div>
    <!-- <script src="/js/signup.js"></script> -->
</body>
</html>